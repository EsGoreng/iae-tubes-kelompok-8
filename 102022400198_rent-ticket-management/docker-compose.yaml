version: '3.8'

services:
  ticket-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dawai-ticket-service
    ports:
      - "8002:8002"
    volumes:
      - .:/var/www
    networks:
      - rent-network

networks:
  rent-network:
    driver: bridge